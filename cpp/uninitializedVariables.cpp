#include <iostream>

using namespace std;

int main (void) {
    // Compiling with the below command will return error.
    // It should be the ideal way to compile any cpp file.
    // g++ <file-name> -Wall -Weffc++ -Wextra -Wsign-conversion
    int x;
    cout << x << endl;

    return (0);
}
